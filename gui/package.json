{
  "name": "zappy-gui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zappy graphic client (TypeScript + HTML5 Canvas)",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint . && tsc --noEmit",
    "test": "tsc --noEmit && tsc -p tsconfig.build.json && node --test dist/g02.test.js dist/g03.test.js dist/g04.test.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  }
}
